# Ansible generated file. Do NOT edit.

{% for ups in ups_devices %}
[ups-{{ loop.index }}]
  driver = usbhid-ups
  port = auto
  desc = "{{ ups }}"
{% endfor %}

{% if heartbeat_enabled %)
[ups-H]
  driver = dummy-ups
  port = ups-heartbeat.conf
  desc = "UPS Heartbeat"
{% endif %)
